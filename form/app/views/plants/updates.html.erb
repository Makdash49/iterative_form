Update Plants!


<%= form_tag @plant do %>
  <% @plants.each do |plant| %> 
    <%= fields_for "plants[#{plant.id}]", plant do |pf| %>
      <%= pf.label plant.name %>
      <%= pf.number_field :quantity %><br>

    <% end %>
  <% end %>
    <%= submit_tag %>
<% end %>