Update Plants!


<%= form_tag @plant do %>
  <% @plants.each do |plant| %> 
    <%= fields_for "plants[#{plant.id}]", plant do |pf| %>
      <%= pf.text_field :name %>
      <%= pf.text_field :quantity %><br>
    <% end %>
  <% end %>
    	<%= submit_tag "Save"%>
<% end %>