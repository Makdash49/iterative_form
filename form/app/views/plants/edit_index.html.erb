Plants and Quantities:<br><br>

<%= form_tag @plant do %>
  <% @plants.each do |plant| %> 
    <%= fields_for "plants[#{plant.id}]", plant do |pf| %>
      <%= pf.label plant.name %><br>
      <%= pf.number_field :quantity %><br><br>
    <% end %>
  <% end %>
    <%= submit_tag %>
<% end %>